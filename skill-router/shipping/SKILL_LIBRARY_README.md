# Skill library

Place skill directories here. Each directory must contain a `SKILL.md` file
with YAML frontmatter containing at least `name` and `description`.

Run `..\index-skills.ps1` from PowerShell after adding or changing skills.
